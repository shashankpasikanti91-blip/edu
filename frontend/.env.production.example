# ============================================================
# SRP Education AI — Frontend Production Environment
# ============================================================
# This file is embedded into the Docker build via ARG.
# The NEXT_PUBLIC_API_URL is baked at build time.
# ============================================================

NEXT_PUBLIC_API_URL=https://edu.srpailabs.com/api/v1
NEXT_PUBLIC_APP_NAME=SRP Education AI
